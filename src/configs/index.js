import formInitialization from './form.config';
import menuList from './menu.config';
import routes from './routes.config';
import headTableColumns from './table.config';

const configs = {
    formInitialization,
    menuList,
    routes,
    headTableColumns
}

export default configs